# Colab provides a mutually compatible torch/torchvision/CUDA stack.  Keep it
# in place and install these non-CUDA dependencies plus both editable packages
# with --no-deps, as shown in the notebook.
numpy>=1.26,<3
opencv-python-headless>=4.8,<5
PyYAML>=6,<7
tensorboard>=2.16,<3
onnx>=1.16,<2
onnxscript>=0.4,<1
onnxruntime-gpu>=1.18,<2
pytest>=7.4,<10
